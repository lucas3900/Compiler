
class A {
  int fact(int x) 
  { 
    if (x == 0)
      return 1;
    return x * fact(x-1);
  }
}

class B extends A {
  double fact(double x) 
  { 
    if (x == 0.0)
      return 0.0;
    return x + fact(x-1);
  }
}

void main()
{
  B a;
  a = New(B);
  B();
}

